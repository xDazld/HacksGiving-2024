<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModels="clr-namespace:Aha.ViewModels"
    x:Class="Aha.Views.InitialContextQuiz.Q3"
    xmlns:resx="clr-namespace:Aha.Resources.Localization"
    xmlns:strings="Aha.Resources"
    Title=""
    BackgroundColor="White">

    <!-- Set the BindingContext to the ContextQuizViewModel -->
    <ContentPage.BindingContext>
        <viewModels:ContextQuizViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <Grid>

            <!-- Content -->
                <ScrollView>
                    <StackLayout 
                    Padding="30"
                    Spacing="20"
                    VerticalOptions="Center"
                    HorizontalOptions="CenterAndExpand">

                        <Label 
                        Text="{x:Static resx:AppResources.Q3SelectInterests1}"
                        FontSize="36"
                        FontAttributes="Bold"
                        TextColor="Black"
                        HorizontalOptions="Center" />

                        <Label 
                        Text="{x:Static resx:AppResources.Q3SelectInterests2}"
                        FontSize="24"
                        TextColor="Black"
                        HorizontalTextAlignment="Center"
                        HorizontalOptions="Center"
                        Margin="0,0,0,20" />

                        <!-- Interests Selection -->
                        <Grid 
                        ColumnDefinitions="*, *" 
                        RowDefinitions="Auto" 
                        ColumnSpacing="10" 
                        RowSpacing="10"
                        HorizontalOptions="CenterAndExpand">

                            <!-- First Column -->
                            <StackLayout Grid.Column="0" Spacing="15">
                                <StackLayout Orientation="Horizontal" Spacing="10">
                                    <CheckBox 
                                    IsChecked="{Binding Science}" 
                                    Color="#346beb" />
                                    <Label 
                                    Text="{x:Static resx:AppResources.Q3Science}"
                                    FontSize="20"
                                    TextColor="Black"
                                    VerticalOptions="Center" />
                                </StackLayout>

                                <StackLayout Orientation="Horizontal" Spacing="10">
                                    <CheckBox 
                                    IsChecked="{Binding Geography}" 
                                    Color="#346beb" />
                                    <Label 
                                    Text="{x:Static resx:AppResources.Q3Geography}"
                                    FontSize="20"
                                    TextColor="Black"
                                    VerticalOptions="Center" />
                                </StackLayout>

                                <StackLayout Orientation="Horizontal" Spacing="10">
                                    <CheckBox 
                                    IsChecked="{Binding History}" 
                                    Color="#346beb" />
                                    <Label 
                                    Text="{x:Static resx:AppResources.Q3History}"
                                    FontSize="20"
                                    TextColor="Black"
                                    VerticalOptions="Center" />
                                </StackLayout>

                                <StackLayout Orientation="Horizontal" Spacing="10">
                                    <CheckBox 
                                    IsChecked="{Binding Oceans}" 
                                    Color="#346beb" />
                                    <Label 
                                    Text="{x:Static resx:AppResources.Q3Oceans}"
                                    FontSize="20"
                                    TextColor="Black"
                                    VerticalOptions="Center" />
                                </StackLayout>
                            </StackLayout>

                            <!-- Second Column -->
                            <StackLayout Grid.Column="1" Spacing="15">
                                <StackLayout Orientation="Horizontal" Spacing="10">
                                    <CheckBox 
                                    IsChecked="{Binding Automation}" 
                                    Color="#346beb" />
                                    <Label 
                                    Text="{x:Static resx:AppResources.Q3Automation}"
                                    FontSize="20"
                                    TextColor="Black"
                                    VerticalOptions="Center" />
                                </StackLayout>

                                <StackLayout Orientation="Horizontal" Spacing="10">
                                    <CheckBox 
                                    IsChecked="{Binding Technology}" 
                                    Color="#346beb" />
                                    <Label 
                                    Text="{x:Static resx:AppResources.Q3Technology}"
                                    FontSize="20"
                                    TextColor="Black"
                                    VerticalOptions="Center" />
                                </StackLayout>

                                <StackLayout Orientation="Horizontal" Spacing="10">
                                    <CheckBox 
                                    IsChecked="{Binding Engineering}" 
                                    Color="#346beb" />
                                    <Label 
                                    Text="{x:Static resx:AppResources.Q3Engineering}"
                                    FontSize="20"
                                    TextColor="Black"
                                    VerticalOptions="Center" />
                                </StackLayout>

                                <StackLayout Orientation="Horizontal" Spacing="10">
                                    <CheckBox 
                                    IsChecked="{Binding Art}" 
                                    Color="#346beb" />
                                    <Label 
                                    Text="{x:Static resx:AppResources.Q3Art}"
                                    FontSize="20"
                                    TextColor="Black"
                                    VerticalOptions="Center" />
                                </StackLayout>

                                <StackLayout Orientation="Horizontal" Spacing="10">
                                    <CheckBox 
                                    IsChecked="{Binding Music}" 
                                    Color="#346beb" />
                                    <Label 
                                    Text="{x:Static resx:AppResources.Q3Music}"
                                    FontSize="20"
                                    TextColor="Black"
                                    VerticalOptions="Center" />
                                </StackLayout>
                            </StackLayout>

                        </Grid>

                        <!-- Finish Button -->
                        <Button 
                        Text="{x:Static resx:AppResources.QFinish}" 
                        Command="{Binding SaveContextCommand}"
                        FontSize="20"
                        TextColor="Black"
                        BackgroundColor="#346beb"
                        CornerRadius="25"
                        HeightRequest="50"
                        WidthRequest="200"
                        Margin="0,30,0,0"
                        HorizontalOptions="Center" />

                    </StackLayout>
                </ScrollView>
        </Grid>
    </ContentPage.Content>
</ContentPage>
